name: Deploy To Azure

on:
  push:
    branches:    
      - dev

jobs:
  create-azure-resources:
    runs-on: ubuntu-latest
    steps:
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Create Metadata Folder
      run: mkdir -p metadata

    - name: Create Storage Account (Web App)
      uses: azure/CLI@v1
      env:
        STORAGE_ACCOUNT_NAME: ${{ secrets.AZURE_STORAGE_ACCOUNT_NAME }}
        RESOURCE_GROUP: ${{ secrets.AZURE_RESOURCE_GROUP }}
        REGION: ${{ secrets.AZURE_REGION }}
      with:
        azcliversion: latest
        inlineScript: |
          az storage account create -n "$STORAGE_ACCOUNT_NAME" -g "$RESOURCE_GROUP" --sku Standard_LRS -l "$REGION"

    - name: Configure Static Website
      uses: azure/CLI@v1
      env:
        STORAGE_ACCOUNT_NAME: ${{ secrets.AZURE_STORAGE_ACCOUNT_NAME }}
      with:
        azcliversion: latest
        inlineScript: |
          az storage blob service-properties update --account-name "$STORAGE_ACCOUNT_NAME" --static-website --404-document "404.html" --index-document "index.html"
          webapp_url=$(az storage account show -n githubactionsdemowebapp -g githubactionsdemo --query primaryEndpoints.web -o tsv)
          echo $webapp_url >> metadata/webapp_url.txt
          echo "::set-output name=webapp_url::$webapp_url"

    - name: Create Storage Account (Functions App)
      uses: azure/CLI@v1
      env:
        STORAGE_ACCOUNT_NAME: ${{ secrets.AZURE_FUNCTIONS_APP_STORAGE }}
        RESOURCE_GROUP: ${{ secrets.AZURE_RESOURCE_GROUP }}
        REGION: ${{ secrets.AZURE_REGION }}
      with:
        azcliversion: latest
        inlineScript: |
          az storage account create -n "$STORAGE_ACCOUNT_NAME" -g "$RESOURCE_GROUP" --sku Standard_LRS -l "$REGION"

    - name: Create Functions App
      uses: azure/CLI@v1
      env:
        FUNCTIONS_APP_NAME: ${{ secrets.AZURE_FUNCTIONS_APP_NAME }}
        STORAGE_ACCOUNT_NAME: ${{ secrets.AZURE_FUNCTIONS_APP_STORAGE }}
        RESOURCE_GROUP: ${{ secrets.AZURE_RESOURCE_GROUP }}
        REGION: ${{ secrets.AZURE_REGION }}
      with:
        azcliversion: latest
        inlineScript: |
          az functionapp create -n "$FUNCTIONS_APP_NAME" -g "$RESOURCE_GROUP" --consumption-plan-location "$REGION" -s "$STORAGE_ACCOUNT_NAME" --functions-version 3 --runtime dotnet 
          az functionapp show -n "$FUNCTIONS_APP_NAME" -g "$RESOURCE_GROUP" > metadata/azure-function-app.json
          functionapp_key=$(az functionapp keys list -n githubactionsdemo-func -g githubactionsdemo --query functionKeys.default -o tsv)
          echo $functionapp_key >> metadata/functionapp_key.txt

    - name: Store Metadata
      uses: actions/upload-artifact@v2
      with:
        name: metadata
        path: ./metadata/

  build-webapp:
    needs: create-azure-resources
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]

    steps:
      - uses: actions/checkout@v1

      - name: Get Metadata
        uses: actions/download-artifact@v2
        with:
          name: metadata
          path: ./metadata/

      - name: Cache node modules
        uses: actions/cache@v1
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Node ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      - name: Configure Prod Environment File
        env:
          FUNCTIONS_APP_NAME: ${{ secrets.AZURE_FUNCTIONS_APP_NAME }}
        run: |
          functionapp_key_escaped=$(cat ./metadata/functionapp_key.txt | sed -e 's/[\/&]/\\&/g')
          cat ./webapp/src/environments/environment.prod.ts \
            sed "s/<name-of-your-functions-app>/$FUNCTIONS_APP_NAME/g" \
            sed "s/<key-of-your-functions-app>/$functionapp_key_escaped/g" \
            > ./webapp/src/environments/environment.prod.ts

      - name: npm ci and npm run build
        working-directory: ./webapp
        run: |
          npm ci
          npm run build:ci

      - name: Store Website
        uses: actions/upload-artifact@v2
        with:
          name: static-webapp
          path: ./webapp/dist/

  deploy-webapp:
    needs: [build-webapp, create-azure-resources]
    runs-on: ubuntu-latest

    steps:
    - name: Get Website
      uses: actions/download-artifact@v2
      with:
        name: static-webapp

    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Upload Files
      uses: azure/CLI@v1
      env:
        STORAGE_ACCOUNT_NAME: ${{ secrets.AZURE_STORAGE_ACCOUNT_NAME }}
      with:
        azcliversion: latest
        inlineScript: |
          az storage blob sync -s ./webapp -c '$web' --account-name "$STORAGE_ACCOUNT_NAME"
